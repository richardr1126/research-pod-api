name: research-pod

services:
  research-api:
    build: .
    volumes:
      - ./research/scraper/pdfs:/app/research/scraper/pdfs
    ports:
      - "8888:8080"
    env_file:
      - ./.env
    restart: unless-stopped
  # web-api:
  #   build: ./web/
  #   ports:
  #     - "8080:8080"
  #   env_file:
  #     - ./web/.env
  #   restart: unless-stopped